/// Web detection for the pure-Dart SIP layer (no Flutter dep).
///
/// Default value is for the `dart.library.html` target (web). The IO
/// override in `is_web_io.dart` reports `false`.
library;

const bool isWeb = true;
